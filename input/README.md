# input/

Put the monthly data pack here (one Excel workbook, `.xlsx`).

Nothing in this folder is committed — project data stays with the project. The tools read from here and never write here.

Expected: `AI2C_Day2_Monthly_Report_Data_Pack_Demo.xlsx` (13 sheets) for the standard run. Any workbook with the same sheet layout works.
