# Workflows

Workflows are Markdown SOPs for repeatable work. Each workflow should define:

- Objective
- Required inputs
- Tools to use
- Procedure
- Expected outputs
- Verification steps
- Edge cases and recovery notes

Create or overwrite workflow files only when the user asks for that workflow to be created or updated.

